const cars =["honda" , "Fiat" , "Dodge" , "ford" , "Chevy"]
const lowerCars=[]
for (let i=0; i <cars.length; i++) {
  lowerCars.push(cars[i].toLowerCase())
}
for (let i=0, i <lowerCars.length; i++) {
  console.log(cars[i][0],(cars[i][cars[i].length]-1))
}
const myName="Dean Gaggero"
const myNameLetters= myName.split("")
for (let i=0; i<myNameLetters.length; i++){
  console.log(myNameLetters[i]);
}
let reconstructedName= ""
for (let i=0; i<myNameLetters.length; i++) {
if  (myNameLetters[i]==="d"){

}
}
